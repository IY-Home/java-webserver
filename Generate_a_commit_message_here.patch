Subject: [PATCH] Generate a commit message here
---
Index: pom.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/pom.xml b/pom.xml
--- a/pom.xml	(revision 1aac5025f332324edffed89335c618e3ccd766a7)
+++ b/pom.xml	(date 1785914417012)
@@ -4,9 +4,9 @@
          xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
     <modelVersion>4.0.0</modelVersion>
 
-    <groupId>org.example</groupId>
+    <groupId>com.youfuns</groupId>
     <artifactId>WebServer</artifactId>
-    <version>1.0-SNAPSHOT</version>
+    <version>1.1.0</version>
 
     <properties>
         <maven.compiler.source>25</maven.compiler.source>
Index: .gitignore
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.gitignore b/.gitignore
--- a/.gitignore	(revision 1aac5025f332324edffed89335c618e3ccd766a7)
+++ b/.gitignore	(date 1785914395293)
@@ -40,5 +40,5 @@
 
 public/
 templates/
-src/main/java/com/youfuns/webserver/demo/
 uploads/
+/src/main/java/com/youfuns/webserver/demo/test/
Index: src/main/java/com/youfuns/webserver/Exchange.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/main/java/com/youfuns/webserver/Exchange.java b/src/main/java/com/youfuns/webserver/Exchange.java
--- a/src/main/java/com/youfuns/webserver/Exchange.java	(revision 1aac5025f332324edffed89335c618e3ccd766a7)
+++ b/src/main/java/com/youfuns/webserver/Exchange.java	(date 1785630952561)
@@ -125,6 +125,11 @@
         return headers.get(name);
     }
 
+    public Map<String, List<String>> getRequestHeaderMap() {
+        Map<String, List<String>> headerMap = headers;
+        return headerMap;
+    }
+
     public boolean hasRequestHeader(String name) {
         return headers.containsKey(name);
     }
@@ -681,7 +686,7 @@
         return savePath.toString();
     }
 
-    public void serveFile(String filePath) {
+    public void serveFile(String filePath) throws IOException {
         String expandedPath = filePath.replace("~", System.getProperty("user.home"));
         Path file = Paths.get(expandedPath);
         if (!Files.exists(file) || Files.isDirectory(file)) {
@@ -707,6 +712,7 @@
             }
         } catch (IOException e) {
             logger.log(WebServer.class, "Failed to serve file: " + e.getMessage(),  SimpleLogger.Level.ERROR);
+            throw e;
         };
     }
 
Index: src/main/java/com/youfuns/webserver/demo/Main.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/main/java/com/youfuns/webserver/demo/Main.java b/src/main/java/com/youfuns/webserver/demo/Main.java
--- a/src/main/java/com/youfuns/webserver/demo/Main.java	(revision 1aac5025f332324edffed89335c618e3ccd766a7)
+++ b/src/main/java/com/youfuns/webserver/demo/Main.java	(date 1785630514218)
@@ -1,10 +1,12 @@
 package com.youfuns.webserver.demo;
 
 import com.youfuns.logger.LoggerManager;
+import com.youfuns.webserver.Exchange;
 import com.youfuns.webserver.TemplateEngine;
 import com.youfuns.webserver.WebServer;
 
 public class Main {
+    // Testing the webserver
     public static void main(String[] args) {
         WebServer myServer = new WebServer(8080);
         myServer.on("/greet", exchange -> {
@@ -15,19 +17,52 @@
                     exchange.addResponseHeader("Content-Type", "text/html");
                     exchange.sendResponse(engine.getTemplate());
                 })
-                .on("/setLogo", (exchange) -> {
-                    LoggerManager.quickLog(exchange.getQueryParameters().get("path"));
-                    try {
-                        myServer.serveFile("/logo", exchange.getQueryParameter("path", "./logos/default.png"));
-                    } catch (IllegalArgumentException e) {
-                        exchange.sendBadRequestResponse("File was not found");
+                .on("/api/setConfig", "POST", exchange -> {
+                    if (!exchange.isMultipartRequest()) {
+                        exchange.sendBadRequestResponse("Expected multipart/form-data");
+                        return;
+                    }
+
+                    if (!exchange.hasFile("config")) {
+                        exchange.sendBadRequestResponse("No logo uploaded");
+                        return;
                     }
-                    exchange.sendResponse("Updated successfully");
+
+                    Exchange.UploadedFile file = exchange.getFile("config");
+
+                    // Check file type
+                    if (exchange.isJSON(file)) {
+                        exchange.saveFileSafe(file, "./config", false); // Don't overwrite previous configs for debug
+                        exchange.sendResponse("Uploaded: " + file.getFilename());
+                        myServer.serveFile("/config", "./config");
+
+                    } else {
+                        exchange.sendBadRequestResponse("Only JSON allowed");
+                    }
                 })
                 .hook(exchange -> {
-                            LoggerManager.quickLog("Received " + exchange.getHttpMethod() + " to " + exchange.getRequestPath());
-                            return true;
-                        }
-                ).start();
+                    LoggerManager.quickLog("Received " + exchange.getHttpMethod() + " to " + exchange.getRequestPath());
+                    return true;
+                })
+                .hook(exchange -> {
+                    exchange.setAttribute("startTime", System.nanoTime());
+                    return true;
+                })
+                .tail(exchange -> {
+                    Long startTime = exchange.getAttribute("startTime", Long.class);
+                    if (startTime != null) {
+                        long duration = (System.nanoTime() - startTime) / 1_000_000; // milliseconds
+                        LoggerManager.quickLog("Duration taken: " + duration);
+                    }
+                    return true;
+                })
+                .onException((exchange, exception) -> {
+                    LoggerManager.quickLog("Caught " + exception.getClass().getSimpleName() + ": " + exception.getMessage());
+                    if (exception instanceof IllegalArgumentException) {
+                        exchange.sendBadRequestResponse("Bad request: " + exception.getMessage());
+                    } else {
+                        exchange.sendErrorResponse("An error occurred.");
+                    }
+                }).start();
     }
 }
\ No newline at end of file
Index: README.md
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/README.md b/README.md
--- a/README.md	(revision 1aac5025f332324edffed89335c618e3ccd766a7)
+++ b/README.md	(date 1785625277758)
@@ -631,9 +631,13 @@
                 return true;
             })
             .onException((exchange, exception) -> {
-                    Logger.log("Caught " + exception.getClass().getSimpleName() + ": " + exception.getMessage());
+                Logger.log("Caught " + exception.getClass().getSimpleName() + ": " + exception.getMessage());
+                if (exception instanceof IllegalArgumentException) {
+                    exchange.sendBadRequestResponse("Bad request: " + exception.getMessage());
+                } else {
                     exchange.sendErrorResponse("An error occurred.");
-                })
+                }
+            })
             .limitUploadSize(50 * 1024 * 1024) // 50 MB
             .start();
     }
Index: .idea/misc.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/misc.xml b/.idea/misc.xml
--- a/.idea/misc.xml	(revision 1aac5025f332324edffed89335c618e3ccd766a7)
+++ b/.idea/misc.xml	(date 1785914669160)
@@ -1,5 +1,8 @@
 <?xml version="1.0" encoding="UTF-8"?>
 <project version="4">
+  <component name="Black">
+    <option name="pathToExecutable" value="/opt/homebrew/bin/black" />
+  </component>
   <component name="ExternalStorageConfigurationManager" enabled="true" />
   <component name="MavenProjectsManager">
     <option name="originalFiles">
Index: .idea/vcs.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/.idea/vcs.xml b/.idea/vcs.xml
--- a/.idea/vcs.xml	(revision 1aac5025f332324edffed89335c618e3ccd766a7)
+++ b/.idea/vcs.xml	(date 1785914669163)
@@ -1,5 +1,10 @@
 <?xml version="1.0" encoding="UTF-8"?>
 <project version="4">
+  <component name="CommitMessageInspectionProfile">
+    <profile version="1.0">
+      <inspection_tool class="SubjectBodySeparation" enabled="true" level="WARNING" enabled_by_default="true" />
+    </profile>
+  </component>
   <component name="VcsDirectoryMappings">
     <mapping directory="" vcs="Git" />
   </component>
Index: src/main/java/com/youfuns/webserver/InternalHomeHandler.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/main/java/com/youfuns/webserver/InternalHomeHandler.java b/src/main/java/com/youfuns/webserver/InternalHomeHandler.java
--- a/src/main/java/com/youfuns/webserver/InternalHomeHandler.java	(revision 1aac5025f332324edffed89335c618e3ccd766a7)
+++ b/src/main/java/com/youfuns/webserver/InternalHomeHandler.java	(date 1785631156567)
@@ -39,7 +39,7 @@
         if (address == null || address.isEmpty() || address.equals("/")) {
             dynamicHandler.handle(exchange);
         } else {
-            if (notFound == null) exchange.sendNotFoundResponse();
+            if (notFound == null) { exchange.sendNotFoundResponse(); return; }
             notFound.handle(exchange);
         }
     }
